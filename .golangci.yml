linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - typecheck
    - gofmt

run:
  timeout: 3m

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
